@model ReportedTripsViewModel

<section class="row" id="reportedTrips">
    <table class="highlight">
        <thead>
            <tr>
                <th data-field="id" class="center-align">Пътуване</th>
                <th data-field="name" class="center-align">Действие</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var trip in Model.ReportedTrips)
            {
                <tr>
                    <td>
                        <div class="reported-trip-container">
                            @Html.Partial("_TripInfo", trip)
                        </div>
                    </td>
                    <td>
                        @Ajax.ActionLink("Изтрий", nameof(AdminController.DeleteTrip), new { tripId = trip.Id },
                       new AjaxOptions() { HttpMethod = "POST", UpdateTargetId = "reportedTrips" }, new { @class = "btn red" })

                        @Ajax.ActionLink("Възстанови", nameof(AdminController.UnReportTrip), new { tripId = trip.Id },
                       new AjaxOptions() { HttpMethod = "POST", UpdateTargetId = "reportedTrips" }, new { @class = "btn" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>
